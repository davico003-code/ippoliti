// Re-export the hook from the provider so consumers can import it from a
// stable, hook-only module without pulling the provider's full client tree.
export { useGalleryLightbox } from './GalleryLightboxProvider'
