// Sección 4 (desktop). El contenido vive en RecursosCalculadoras (responsive,
// compartido con mobile). Wrapper fino para mantener el árbol desktop.

import RecursosCalculadoras from './recursos/RecursosCalculadoras'

export default function GuiaDesktop() {
  return <RecursosCalculadoras />
}
